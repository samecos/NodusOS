# R1.4 类型关系建模 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Nodus 的符号索引中完整建模类型关系：class 继承、interface 实现、type 使用，并支持 "谁实现了这个接口" / "哪些类继承了它" / "哪些类型引用了它" 三类自然语言查询。

**Architecture:** 扩展 `ReferenceKind` 新增 `interface_implements`；让 `TypeScriptParser` / `PythonParser` 在索引阶段把 `extends` / `implements` / class base 解析为带正确 kind 的引用；在 `CodeIntelligence` 中新增 `findSubclasses` / `findImplementations` / `findTypeUses` 三个查询方法；通过 `IntentEngine` 新增 `type_relationships` 意图并路由到 `CodeIntelligence.query`；`TerminalRenderer` 新增对应的结果卡片渲染。

**Tech Stack:** Node.js 20+、TypeScript 5.x、tree-sitter、better-sqlite3、Vitest。

## Global Constraints

- ESM，`"type": "module"`；类型导入必须写 `import type { ... }`。
- TypeScript：`strict: true`、`verbatimModuleSyntax: true`。
- 测试：每次提交前 `npm test` 必须全绿。
- 代码注释主要使用中文，标识符保持英文。
- 允许新增 `ReferenceKind` / `IntentType` / `QueryResult` 变体，但**不修改数据库 schema**（`refs.kind` 已是 TEXT，可以存入新字符串）。
- TDD：每个 task 先写失败测试，再写最小实现。
- 每个 task 独立可 review，独立可测试。

## File Structure

| 文件 | 职责 |
|------|------|
| `src/common/types.ts` | 扩展 `ReferenceKind`（`interface_implements`）、`IntentType`（`type_relationships`）、新增 `TypeRelationship` / `RelationshipKind` 类型。 |
| `src/code-intel/parsers/typescript-parser.ts` | 区分 `extends_clause` / `implements_clause`，发射 `inheritance` / `interface_implements` 引用。 |
| `src/code-intel/parsers/python-parser.ts` | 为 Python class 的 base 列表发射 `inheritance` 引用。 |
| `src/code-intel/code-intelligence.ts` | 接口新增 `findSubclasses` / `findImplementations` / `findTypeUses` 和 `type_relationship_list` QueryResult。 |
| `src/code-intel/code-intelligence.impl.ts` | 实现三类查询方法和 `query` 路由。 |
| `src/code-intel/code-analytics.ts` | 新增 `typeRelationships(symbolId, kind)` 分析接口。 |
| `src/code-intel/code-analytics.impl.ts` | 实现 `typeRelationships`。 |
| `src/intent/intent-engine.ts` | `IntentEntity` 新增 `relationshipKind?`。 |
| `src/intent/intent-engine.impl.ts` | 新增 `type_relationships` 意图模式与例句。 |
| `src/ui/ui-renderer.ts` | `CardKind` 新增 `type_relationship_list`。 |
| `src/ui/terminal-renderer.ts` | 渲染 `type_relationship_list`。 |
| 各 `*.test.ts` | 单元/集成测试。 |
| `readme.md` / `ArchitecturalDesignPhase/05-Future-Roadmap.md` / `04-API-Reference.md` | 文档更新。 |

---

## Task 1: 扩展 ReferenceKind 与 TypeScriptParser 类型关系引用

**Files:**
- Modify: `src/common/types.ts`
- Modify: `src/code-intel/parsers/typescript-parser.ts`
- Test: `src/code-intel/code-intelligence.test.ts`

**Interfaces:**
- Consumes: existing `ReferenceKind`, `SymbolKind`
- Produces: refs with `kind: 'inheritance' | 'interface_implements'`

- [ ] **Step 1: 写失败测试**

在 `src/code-intel/code-intelligence.test.ts` 末尾追加：

```typescript
  // TC-UT-CI-027: class extends class 产生 inheritance 引用
  it('TC-UT-CI-027: class extends class emits inheritance reference', () => {
    const source = `
export class Base {}
export class Derived extends Base {}
`;
    const symbols = parser.parseSymbols(source, 'src/derived.ts');
    const refs = parser.parseReferences(source, symbols);
    const inheritRefs = refs.filter(r => r.kind === 'inheritance');
    expect(inheritRefs.length).toBeGreaterThanOrEqual(1);
    expect(inheritRefs[0]!.target_symbol_id).toBe(symbols.find(s => s.name === 'Base')!.id);
  });

  // TC-UT-CI-028: class implements interface 产生 interface_implements 引用
  it('TC-UT-CI-028: class implements interface emits interface_implements reference', () => {
    const source = `
export interface IUserService {}
export class UserService implements IUserService {}
`;
    const symbols = parser.parseSymbols(source, 'src/service.ts');
    const refs = parser.parseReferences(source, symbols);
    const implRefs = refs.filter(r => r.kind === 'interface_implements');
    expect(implRefs.length).toBeGreaterThanOrEqual(1);
    expect(implRefs[0]!.target_symbol_id).toBe(symbols.find(s => s.name === 'IUserService')!.id);
  });

  // TC-UT-CI-029: interface extends interface 产生 inheritance 引用
  it('TC-UT-CI-029: interface extends interface emits inheritance reference', () => {
    const source = `
export interface Base {}
export interface Derived extends Base {}
`;
    const symbols = parser.parseSymbols(source, 'src/derived.ts');
    const refs = parser.parseReferences(source, symbols);
    const inheritRefs = refs.filter(r => r.kind === 'inheritance');
    expect(inheritRefs.length).toBeGreaterThanOrEqual(1);
  });
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npm test -- src/code-intel/code-intelligence.test.ts
```

Expected: 三个新增用例失败（`interface_implements` 未定义或引用 kind 不匹配）。

- [ ] **Step 3: 扩展 ReferenceKind**

在 `src/common/types.ts` 中：

```typescript
/** 引用类型 */
export type ReferenceKind =
  | 'call'
  | 'import'
  | 'inheritance'
  | 'interface_implements'
  | 'type_use'
  | 'instantiation'
  | 'override'
  | 'decorator_use';
```

- [ ] **Step 4: 修改 TypeScriptParser 区分 extends / implements**

1. 在 `TSNode` 接口中增加 `parent`：

```typescript
interface TSNode {
  type: string;
  text: string;
  startPosition: { row: number; column: number };
  endPosition: { row: number; column: number };
  children: TSNode[];
  namedChildren: TSNode[];
  parent: TSNode | null;
  childForFieldName?(name: string): TSNode | null;
  isNamed(): boolean;
}
```

2. 替换 `walkForRefs` 中继承/实现分支：

```typescript
    // 继承关系
    if (node.type === 'extends_clause' || node.type === 'implements_clause') {
      const parentDecl = node.parent;
      const kind: ReferenceKind = node.type === 'implements_clause'
        ? 'interface_implements'
        : 'inheritance';

      for (const typeNode of node.namedChildren) {
        if (typeNode.type !== 'type_identifier' && typeNode.type !== 'identifier') continue;
        const name = typeNode.text;
        const target = symbolMap.get(name);
        refs.push({
          id: hashSymbolId(filePath, `${kind}_${name}`, kind, typeNode.startPosition.row + 1),
          source_symbol_id: '',
          target_symbol_id: target?.id ?? `external:${name}`,
          location: {
            file_path: filePath,
            line_start: typeNode.startPosition.row + 1, line_end: typeNode.endPosition.row + 1,
            col_start: typeNode.startPosition.column + 1, col_end: typeNode.endPosition.column + 1,
          },
          kind,
        });
      }
    }
```

- [ ] **Step 5: 运行测试确认通过**

```bash
npm test -- src/code-intel/code-intelligence.test.ts
```

Expected: 全部通过。

- [ ] **Step 6: 提交**

```bash
git add src/common/types.ts src/code-intel/parsers/typescript-parser.ts src/code-intel/code-intelligence.test.ts
git commit -m "feat(code-intel): distinguish inheritance and interface_implements references"
```

---

## Task 2: PythonParser 类继承引用

**Files:**
- Modify: `src/code-intel/parsers/python-parser.ts`
- Test: `src/code-intel/code-intelligence.test.ts`（已有 Python 测试区域）

**Interfaces:**
- Consumes: existing `ReferenceKind`
- Produces: refs with `kind: 'inheritance'` for Python class bases

- [ ] **Step 1: 写失败测试**

在 `src/code-intel/code-intelligence.test.ts` 的 `PythonParser` describe 中追加：

```typescript
  // TC-UT-CI-030: Python class 继承产生 inheritance 引用
  it('TC-UT-CI-030: Python class inheritance emits inheritance reference', () => {
    const source = `
class Base:
    pass

class Derived(Base):
    pass
`;
    const pyParser = new PythonParser();
    const symbols = pyParser.parseSymbols(source, 'src/models.py');
    const refs = pyParser.parseReferences(source, symbols);
    const inheritRefs = refs.filter(r => r.kind === 'inheritance');
    expect(inheritRefs.length).toBeGreaterThanOrEqual(1);
    expect(inheritRefs[0]!.target_symbol_id).toBe(symbols.find(s => s.name === 'Base')!.id);
  });
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npm test -- src/code-intel/code-intelligence.test.ts
```

Expected: TC-UT-CI-030 失败（无 inheritance 引用）。

- [ ] **Step 3: 实现 Python class base 解析**

在 `src/code-intel/parsers/python-parser.ts` 的 `walkForRefs` 中，于 `import_from_statement` 分支之后添加：

```typescript
    // class 继承: class Derived(Base)
    if (node.type === 'class_definition') {
      const classNameNode = node.childForFieldName?.('name');
      const className = classNameNode?.text;
      const basesNode = node.childForFieldName?.('superclasses');
      if (basesNode && className) {
        for (const base of basesNode.namedChildren) {
          const name = base.type === 'identifier' ? base.text
            : base.type === 'attribute' ? (base.childForFieldName?.('attribute')?.text ?? base.text)
            : base.text;
          const target = symbolMap.get(name);
          refs.push({
            id: `py_inherit_${className}_${name}_${base.startPosition.row + 1}`,
            source_symbol_id: '',
            target_symbol_id: target?.id ?? `py:${name}`,
            location: {
              file_path: filePath,
              line_start: base.startPosition.row + 1,
              line_end: base.endPosition.row + 1,
              col_start: base.startPosition.column + 1,
              col_end: base.endPosition.column + 1,
            },
            kind: 'inheritance',
          });
        }
      }
    }
```

- [ ] **Step 4: 运行测试确认通过**

```bash
npm test -- src/code-intel/code-intelligence.test.ts
```

Expected: TC-UT-CI-030 通过。

- [ ] **Step 5: 提交**

```bash
git add src/code-intel/parsers/python-parser.ts src/code-intel/code-intelligence.test.ts
git commit -m "feat(code-intel): emit inheritance references for Python class bases"
```

---

## Task 3: CodeIntelligence 类型关系查询方法

**Files:**
- Modify: `src/code-intel/code-intelligence.ts`
- Modify: `src/code-intel/code-intelligence.impl.ts`
- Test: `src/code-intel/code-intelligence.test.ts` 或新增 `src/code-intel/type-relationship.test.ts`

**Interfaces:**
- Consumes: `refsFindByTarget(symbolId)` + kind filter
- Produces: `Symbol[]` 和 `{ kind: 'type_relationship_list'; ... }`

- [ ] **Step 1: 写失败测试**

创建 `src/code-intel/type-relationship.test.ts`：

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { SqliteKnowledgeStore } from '../store/knowledge-store.impl.js';
import { CodeIntelligenceImpl } from './code-intelligence.impl.js';
import type { KnowledgeStore } from '../store/knowledge-store.js';
import type { CodeIntelligence } from './code-intelligence.js';

const source = `
export interface IUserService {
  getUser(id: string): void;
}

export class UserService implements IUserService {
  getUser(id: string): void {}
}

export class AdminService implements IUserService {
  getUser(id: string): void {}
}
`;

describe('TypeRelationship queries', () => {
  let store: KnowledgeStore;
  let ci: CodeIntelligence;

  beforeEach(async () => {
    store = new SqliteKnowledgeStore(':memory:');
    ci = new CodeIntelligenceImpl(store);
    const parser = new (await import('./parsers/typescript-parser.js')).TypeScriptParser();
    const symbols = parser.parseSymbols(source, '/tmp/service.ts');
    const refs = parser.parseReferences(source, symbols);
    store.symbolsUpsert(symbols);
    store.refsUpsert(refs);
  });

  it('TC-UT-TR-001: findImplementations returns classes implementing interface', async () => {
    const iface = store.symbolsFindByName('IUserService', 'interface', 1)[0]!;
    const impls = await ci.findImplementations(iface.id);
    expect(impls.map(s => s.name)).toContain('UserService');
    expect(impls.map(s => s.name)).toContain('AdminService');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npm test -- src/code-intel/type-relationship.test.ts
```

Expected: `findImplementations is not a function`。

- [ ] **Step 3: 扩展接口与实现**

在 `src/code-intel/code-intelligence.ts` 中：

1. 新增类型：

```typescript
export type RelationshipKind = 'subclass' | 'implementation' | 'type_use';

export interface TypeRelationship {
  kind: RelationshipKind;
  symbol: Symbol;
}
```

2. 新增到 `QueryResult`：

```typescript
  | { kind: 'type_relationship_list'; root: Symbol; relationships: TypeRelationship[] }
```

3. 在 `CodeIntelligence` 接口中新增：

```typescript
  findSubclasses(symbolId: SymbolId): Promise<Symbol[]>;
  findImplementations(symbolId: SymbolId): Promise<Symbol[]>;
  findTypeUses(symbolId: SymbolId): Promise<Symbol[]>;
```

在 `src/code-intel/code-intelligence.impl.ts` 中实现：

```typescript
  async findSubclasses(symbolId: SymbolId): Promise<Symbol[]> {
    const refs = this.store.refsFindByTarget(symbolId).filter(r => r.kind === 'inheritance');
    return this.symbolsFromRefs(refs);
  }

  async findImplementations(symbolId: SymbolId): Promise<Symbol[]> {
    const refs = this.store.refsFindByTarget(symbolId).filter(r => r.kind === 'interface_implements');
    return this.symbolsFromRefs(refs);
  }

  async findTypeUses(symbolId: SymbolId): Promise<Symbol[]> {
    const refs = this.store.refsFindByTarget(symbolId).filter(r => r.kind === 'type_use');
    return this.symbolsFromRefs(refs);
  }

  private symbolsFromRefs(refs: Reference[]): Symbol[] {
    const seen = new Set<SymbolId>();
    const result: Symbol[] = [];
    for (const ref of refs) {
      if (!ref.source_symbol_id || seen.has(ref.source_symbol_id)) continue;
      const sym = this.store.symbolsFindById(ref.source_symbol_id);
      if (sym) {
        seen.add(ref.source_symbol_id);
        result.push(sym);
      }
    }
    return result;
  }
```

- [ ] **Step 4: 运行测试确认通过**

```bash
npm test -- src/code-intel/type-relationship.test.ts
```

Expected: 1 passed。

- [ ] **Step 5: 提交**

```bash
git add src/code-intel/code-intelligence.ts src/code-intel/code-intelligence.impl.ts src/code-intel/type-relationship.test.ts
git commit -m "feat(code-intel): add findSubclasses/findImplementations/findTypeUses queries"
```

---

## Task 4: CodeAnalytics.typeRelationships 聚合接口

**Files:**
- Modify: `src/code-intel/code-analytics.ts`
- Modify: `src/code-intel/code-analytics.impl.ts`
- Test: `src/code-intel/code-analytics.test.ts`

**Interfaces:**
- Consumes: `SymbolId`, `RelationshipKind`
- Produces: `TypeRelationship[]` (reuse from code-intelligence.ts)

- [ ] **Step 1: 写失败测试**

在 `src/code-intel/code-analytics.test.ts` 中追加：

```typescript
  it('TC-UT-CA-XXX: typeRelationships returns implementations', async () => {
    const analytics = new DefaultCodeAnalytics(store, projectRoot);
    const iface = store.symbolsFindByName('IUserService', 'interface', 1)[0]!;
    const rels = await analytics.typeRelationships(iface.id, 'implementation');
    expect(rels.length).toBeGreaterThanOrEqual(1);
    expect(rels[0].kind).toBe('implementation');
  });
```

需要先确保测试夹具中有 interface 和 implementations。如果当前夹具没有，在 `beforeEach` 或 describe 中补充一个临时项目。

- [ ] **Step 2: 运行测试确认失败**

```bash
npm test -- src/code-intel/code-analytics.test.ts
```

Expected: `typeRelationships is not a function`。

- [ ] **Step 3: 扩展接口与实现**

在 `src/code-intel/code-analytics.ts` 中：

```typescript
import type { TypeRelationship, RelationshipKind } from './code-intelligence.js';

export interface CodeAnalytics {
  // ... 已有方法 ...
  typeRelationships(symbolId: SymbolId, kind: RelationshipKind): Promise<TypeRelationship[]>;
}
```

在 `src/code-intel/code-analytics.impl.ts` 中实现：

```typescript
  async typeRelationships(symbolId: SymbolId, kind: RelationshipKind): Promise<TypeRelationship[]> {
    const kindFilter: Record<RelationshipKind, ReferenceKind> = {
      subclass: 'inheritance',
      implementation: 'interface_implements',
      type_use: 'type_use',
    };
    const refs = this.store.refsFindByTarget(symbolId).filter(r => r.kind === kindFilter[kind]);
    const symbols = this.symbolMap();
    return refs
      .map(r => {
        const sym = symbols.get(r.source_symbol_id);
        return sym ? { kind, symbol: sym } : null;
      })
      .filter((r): r is TypeRelationship => r !== null);
  }
```

- [ ] **Step 4: 运行测试确认通过**

```bash
npm test -- src/code-intel/code-analytics.test.ts
```

Expected: 通过。

- [ ] **Step 5: 提交**

```bash
git add src/code-intel/code-analytics.ts src/code-intel/code-analytics.impl.ts src/code-intel/code-analytics.test.ts
git commit -m "feat(code-intel): add CodeAnalytics.typeRelationships"
```

---

## Task 5: IntentEngine 类型关系意图与 query 路由

**Files:**
- Modify: `src/common/types.ts`
- Modify: `src/intent/intent-engine.ts`
- Modify: `src/intent/intent-engine.impl.ts`
- Modify: `src/code-intel/code-intelligence.impl.ts`
- Test: `src/intent/intent-engine.test.ts`

**Interfaces:**
- Consumes: user natural language
- Produces: `intentType: 'type_relationships'`, entities `{ symbolName, relationshipKind }`

- [ ] **Step 1: 写失败测试**

在 `src/intent/intent-engine.test.ts` 中追加：

```typescript
  it('TC-UT-IE-XXX: should parse "who implements IUserService"', () => {
    const result = engine.parse({ source: 'text', text: '谁实现了 IUserService', locale: 'zh-CN' }, emptyContext);
    expect(result).not.toHaveProperty('kind');
    if (!('kind' in result)) {
      expect(result.intentType).toBe('type_relationships');
      expect(result.entities.symbolName).toBe('IUserService');
      expect(result.entities.relationshipKind).toBe('implementation');
    }
  });
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npm test -- src/intent/intent-engine.test.ts
```

Expected: `type_relationships` 未识别。

- [ ] **Step 3: 扩展类型与接口**

在 `src/common/types.ts` 的 `IntentType` 中追加 `'type_relationships'`：

```typescript
export type IntentType =
  | 'find_definition'
  | 'find_references'
  | 'call_graph'
  | 'impact_analysis'
  | 'change_history'
  | 'symbol_overview'
  | 'list_symbols'
  | 'stats'
  | 'analytics'
  | 'type_relationships';
```

在 `src/intent/intent-engine.ts` 的 `IntentEntity` 中追加：

```typescript
  relationshipKind?: 'subclasses' | 'implementations' | 'type_uses';
```

- [ ] **Step 4: 实现 IntentEngine 模式与例句**

在 `src/intent/intent-engine.impl.ts` 中：

1. `exampleQueries` 追加：

```typescript
    // type_relationships
    { text: '谁实现了 xxx', intentType: 'type_relationships', entityHint: 'symbol' },
    { text: 'xxx 有哪些实现', intentType: 'type_relationships', entityHint: 'symbol' },
    { text: '哪些类继承了 xxx', intentType: 'type_relationships', entityHint: 'symbol' },
    { text: '哪些类型使用了 xxx', intentType: 'type_relationships', entityHint: 'symbol' },
    { text: 'who implements xxx', intentType: 'type_relationships', entityHint: 'symbol' },
    { text: 'subclasses of xxx', intentType: 'type_relationships', entityHint: 'symbol' },
```

2. 在 `matchIntent` 的 `rules` 数组中，在 `analytics` 规则之前插入：

```typescript
      // 类型关系
      {
        patterns: [
          /(?:谁|哪些类|哪些类型)(?:实现|继承|使用|引用).{0,2}(?:了|过)?\s*(.+)/i,
          /(.+?)(?:的|有|有)?(?:哪些)?(?:实现|子类|继承类|使用|引用)/i,
          /(?:who|which\s+classes?)\s+(?:implements?|extends?|uses?)\s+(.+)/i,
          /(?:subclasses?|implementations?)\s+of\s+(.+)/i,
        ],
        intentType: 'type_relationships',
        extractEntities: (_, text, ctx) => ({
          symbolName: this.extractSymbolName(text) ?? ctx.cursor_symbol ?? undefined,
          relationshipKind: this.extractRelationshipKind(text),
        }),
      },
```

3. 新增辅助方法：

```typescript
  private extractRelationshipKind(text: string): 'subclasses' | 'implementations' | 'type_uses' | undefined {
    const lower = text.toLowerCase();
    if (/实现|implements?/.test(lower)) return 'implementations';
    if (/继承|extends?|子类|subclasses?/.test(lower)) return 'subclasses';
    if (/使用|uses?|引用|type\s+uses?/.test(lower)) return 'type_uses';
    return undefined;
  }
```

- [ ] **Step 5: 实现 CodeIntelligence.query 路由**

在 `src/code-intel/code-intelligence.impl.ts` 的 `query` switch 中，于 `default` 之前追加：

```typescript
      case 'type_relationships': {
        const syms = await this.findSymbol(entities.symbolName ?? '', undefined, undefined, 1);
        if (!syms[0]) return { kind: 'type_relationship_list', root: { id: '', name: '', kind: 'interface', language: 'typescript', location: { file_path: '', line_start: 0, line_end: 0, col_start: 0, col_end: 0 }, is_exported: false }, relationships: [] };
        const root = syms[0];
        let related: Symbol[] = [];
        let relKind: RelationshipKind = 'type_use';
        switch (entities.relationshipKind) {
          case 'subclasses':
            related = await this.findSubclasses(root.id);
            relKind = 'subclass';
            break;
          case 'implementations':
            related = await this.findImplementations(root.id);
            relKind = 'implementation';
            break;
          case 'type_uses':
          default:
            related = await this.findTypeUses(root.id);
            relKind = 'type_use';
            break;
        }
        return {
          kind: 'type_relationship_list',
          root,
          relationships: related.map(s => ({ kind: relKind, symbol: s })),
        };
      }
```

需要引入 `RelationshipKind`。

- [ ] **Step 6: 运行测试确认通过**

```bash
npm test -- src/intent/intent-engine.test.ts
npm test -- src/code-intel/type-relationship.test.ts
npm test
```

Expected: 全部通过。

- [ ] **Step 7: 提交**

```bash
git add src/common/types.ts src/intent/intent-engine.ts src/intent/intent-engine.impl.ts src/code-intel/code-intelligence.impl.ts src/intent/intent-engine.test.ts
git commit -m "feat(intent): add type_relationships intent and routing"
```

---

## Task 6: TerminalRenderer 渲染类型关系结果

**Files:**
- Modify: `src/ui/ui-renderer.ts`
- Modify: `src/ui/terminal-renderer.ts`
- Test: `src/ui/terminal-renderer.test.ts`

**Interfaces:**
- Consumes: `QueryResult` with `kind: 'type_relationship_list'`
- Produces: formatted string

- [ ] **Step 1: 写失败测试**

在 `src/ui/terminal-renderer.test.ts` 中追加：

```typescript
  it('TC-UT-TR-XXX: should render type_relationship_list', () => {
    const out = renderer.render({
      kind: 'type_relationship_list',
      root: {
        id: '1', name: 'IUserService', kind: 'interface', language: 'typescript',
        location: { file_path: 'src/service.ts', line_start: 1, line_end: 1, col_start: 1, col_end: 1 },
        is_exported: true,
      },
      relationships: [
        {
          kind: 'implementation',
          symbol: {
            id: '2', name: 'UserService', kind: 'class', language: 'typescript',
            location: { file_path: 'src/service.ts', line_start: 5, line_end: 5, col_start: 1, col_end: 1 },
            is_exported: true,
          },
        },
      ],
    });
    expect(out).toContain('IUserService');
    expect(out).toContain('UserService');
    expect(out).toContain('implementation');
  });
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npm test -- src/ui/terminal-renderer.test.ts
```

Expected: 失败（`type_relationship_list` 未在 switch 中）。

- [ ] **Step 3: 扩展 CardKind 与渲染**

在 `src/ui/ui-renderer.ts` 的 `CardKind` 中追加 `'type_relationship_list'`。

在 `src/ui/terminal-renderer.ts` 中：

1. `render` switch 添加：

```typescript
      case 'type_relationship_list':
        return this.renderTypeRelationships(result.root, result.relationships);
```

2. 新增方法：

```typescript
  private renderTypeRelationships(root: Symbol, relationships: TypeRelationship[]): string {
    if (relationships.length === 0) return c(`\n${root.name} 没有匹配的类型关系`, DIM);

    let out = c(`\n${root.name} 的类型关系:\n`, BOLD);
    for (const rel of relationships) {
      const kindLabel = rel.kind === 'subclass' ? '子类'
        : rel.kind === 'implementation' ? '实现'
        : '类型使用';
      const loc = `${rel.symbol.location.file_path}:${rel.symbol.location.line_start}`;
      out += `\n  ${c(kindLabel, YELLOW)} ${c(rel.symbol.name, BOLD)} ${c(`[${rel.symbol.kind}]`, DIM)}`;
      out += `\n    ${c(loc, BLUE)}`;
    }
    return out + '\n';
  }
```

- [ ] **Step 4: 运行测试确认通过**

```bash
npm test -- src/ui/terminal-renderer.test.ts
npm test
```

Expected: 通过。

- [ ] **Step 5: 提交**

```bash
git add src/ui/ui-renderer.ts src/ui/terminal-renderer.ts src/ui/terminal-renderer.test.ts
git commit -m "feat(ui): render type_relationship_list in terminal"
```

---

## Task 7: 集成测试与文档更新

**Files:**
- Modify: `src/code-intel/code-intelligence.integration.test.ts`
- Modify: `readme.md`
- Modify: `ArchitecturalDesignPhase/05-Future-Roadmap.md`
- Modify: `ArchitecturalDesignPhase/04-API-Reference.md`

- [ ] **Step 1: 写失败集成测试**

在 `src/code-intel/code-intelligence.integration.test.ts` 中新增 describe：

```typescript
describe('CodeIntelligence Type Relationships', () => {
  const tmpDir = join(tmpdir(), `nodus-type-rel-test-${Date.now()}`);
  let store: KnowledgeStore;
  let ci: CodeIntelligence;

  beforeAll(async () => {
    mkdirSync(join(tmpDir, 'src'), { recursive: true });
    writeFileSync(join(tmpDir, 'package.json'), JSON.stringify({ name: 'type-rel-test' }));
    writeFileSync(join(tmpDir, 'src', 'models.ts'), `
export interface IModel {}
export class UserModel implements IModel {}
export class OrderModel implements IModel {}
`);
    writeFileSync(join(tmpDir, 'src', 'app.ts'), `
import { IModel } from './models';
export function save(model: IModel): void {}
`);
    store = new SqliteKnowledgeStore(':memory:');
    ci = new CodeIntelligenceImpl(store);
    await ci.indexProject(tmpDir, ['typescript']);
  });

  afterAll(() => {
    store.close();
    rmSync(tmpDir, { recursive: true, force: true });
  });

  it('TC-IT-CI-TR-001: query type_relationships returns implementations', async () => {
    const result = await ci.query({
      intentType: 'type_relationships',
      confidence: 0.95,
      rawText: '谁实现了 IModel',
      entities: { symbolName: 'IModel', relationshipKind: 'implementations' },
    });
    expect(result.kind).toBe('type_relationship_list');
    if (result.kind === 'type_relationship_list') {
      const names = result.relationships.map(r => r.symbol.name);
      expect(names).toContain('UserModel');
      expect(names).toContain('OrderModel');
    }
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npm test -- src/code-intel/code-intelligence.integration.test.ts
```

Expected: 失败（`type_relationships` 未路由或结果为空）。

- [ ] **Step 3: 验证实现并运行全量测试**

如果 Task 1-6 已实现，则运行：

```bash
npm test -- src/code-intel/code-intelligence.integration.test.ts
npm test
npm run typecheck
```

Expected: 全部通过。

- [ ] **Step 4: 更新文档**

1. `readme.md`：
   - 把 P1 中 `- [ ] R1.4 类型关系建模...` 改为 `- [x]`。
   - 更新 TDD 区块的测试计数为实际数量。

2. `ArchitecturalDesignPhase/05-Future-Roadmap.md`：
   - 在 `CodeIntelligence` 行中删除“继承关系未完整建模”，改为“类型关系已建模（R1.4）”。

3. `ArchitecturalDesignPhase/04-API-Reference.md`：
   - 在 `CodeIntelligence` 接口章节（第 5 节）补充 `findSubclasses` / `findImplementations` / `findTypeUses` 和 `type_relationship_list` QueryResult，标记为 ✅ v1.1。

- [ ] **Step 5: 提交**

```bash
git add src/code-intel/code-intelligence.integration.test.ts readme.md ArchitecturalDesignPhase/05-Future-Roadmap.md ArchitecturalDesignPhase/04-API-Reference.md
git commit -m "docs: mark R1.4 type relationship modeling complete"
```

---

## Self-Review

| 检查项 | 结果 |
|--------|------|
| R1.4 三条查询（实现、子类、类型使用）都有对应 task | ✅ Task 3/5/6 |
| 无数据库 schema 变更 | ✅ 只新增 `refs.kind` 字符串值 |
| 无 placeholder | ✅ 每步含具体代码 |
| 类型一致性：`RelationshipKind` / `TypeRelationship` 在 code-intelligence.ts 定义，analytics/ui/intent 共用 | ✅ |
| 向后兼容：旧的 `find_references` 仍返回所有 kind 的引用 | ✅ |

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-01-r1.4-type-relationship-modeling.md`.

**Execution options:**

1. **Subagent-Driven (recommended)** — dispatch a fresh subagent per task, review between tasks.
2. **Inline Execution** — execute tasks in this session using `superpowers:executing-plans`.

In auto mode, selecting **Option 1: Subagent-Driven**.
