# R1.7: 会话恢复（重启后恢复项目、文件、光标位置）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 SQLite 中新增 `session_state` 表保存每个项目的最后活动文件、光标行/列/符号；在 `NodusShell` 关闭时自动保存当前会话状态，启动 `openProject` 时自动恢复到 `ContextManager`。

**Architecture:** 通过数据库迁移新增 `session_state` 表；在 `KnowledgeStore` 层暴露 `sessionStateGet/Upsert/Remove`；`NodusShell` 在 `shutdown()` 时把 `ContextManager.snapshot()` 写入当前活动项目对应的记录，在 `openProject()` 成功后读取并下发 `project_changed`/`file_opened`/`cursor_moved` 事件完成恢复。

**Tech Stack:** TypeScript, ESM, better-sqlite3, Vitest。

## Global Constraints

- 项目使用 ESM，`package.json` 中 `"type": "module"`。
- `tsconfig.json` 使用 `nodenext`、`strict: true`、`verbatimModuleSyntax: true`；类型导入必须写 `import type { ... }`。
- 测试使用 Vitest，`vitest.config.ts` 已配置 `globals: true`、`environment: 'node'`。
- 所有源码注释与文档主要使用中文；标识符保持英文。
- 测试用例沿用 `TC-UT-STORE-NNN` / `TC-UT-SH-NNN` 编号风格。
- 每次任务结束需运行 `npm test` 与 `npm run typecheck` 并全部通过。
- 每个任务独立可提交，提交信息使用中文或英文 conventional commits。

---

## File Structure

- `src/store/migrations.ts` — 新增 `session_state` 表的迁移（version 2）。
- `src/common/types.ts` — 新增 `SessionState` 类型。
- `src/store/knowledge-store.ts` — 新增 `sessionStateGet/Upsert/Remove` 接口。
- `src/store/knowledge-store.impl.ts` — 实现上述接口。
- `src/store/knowledge-store.test.ts` — 新增会话状态持久化测试。
- `src/shell/nodus-shell.ts` — 在 `shutdown()` 保存会话状态，在 `openProject()` 恢复会话状态。
- `src/shell/nodus-shell.test.ts` — 新增会话恢复集成测试。
- `readme.md` — R1.7 完成后在 TODO 列表打勾。

---

## Task 1: 数据库迁移、`SessionState` 类型与 `KnowledgeStore` 接口

**Files:**
- Modify: `src/store/migrations.ts`
- Modify: `src/common/types.ts`
- Modify: `src/store/knowledge-store.ts`

**Interfaces:**
- Consumes: 现有 `Context` 字段（`active_file`, `cursor_line`, `cursor_col`, `cursor_symbol`）。
- Produces: `SessionState` 类型；`KnowledgeStore` 新增 `sessionStateGet(projectRoot: string): SessionState | undefined`、`sessionStateUpsert(state: SessionState): void`、`sessionStateRemove(projectRoot: string): void`。

- [ ] **Step 1: 写迁移 SQL 并注册版本 2**

在 `src/store/migrations.ts` 的 `MIGRATIONS` 数组中追加第二个迁移（保留 `INITIAL_SCHEMA` 不变）：

```typescript
export const MIGRATIONS: Migration[] = [
  {
    version: 1,
    name: 'initial_schema',
    up: INITIAL_SCHEMA,
  },
  {
    version: 2,
    name: 'add_session_state',
    up: `
      CREATE TABLE IF NOT EXISTS session_state (
        project_root TEXT PRIMARY KEY,
        active_file TEXT,
        cursor_line INTEGER,
        cursor_col INTEGER,
        cursor_symbol TEXT,
        updated_at TEXT NOT NULL DEFAULT (datetime('now'))
      );
      CREATE INDEX IF NOT EXISTS idx_session_state_updated ON session_state(updated_at);
    `,
  },
];
```

- [ ] **Step 2: 添加 `SessionState` 类型**

在 `src/common/types.ts` 中 `QueryHistoryEntry` 之后新增：

```typescript
/** 每个项目的会话状态（最后打开的文件与光标位置） */
export interface SessionState {
  project_root: string;
  active_file: string | null;
  cursor_line: number | null;
  cursor_col: number | null;
  cursor_symbol: string | null;
  updated_at?: string;
}
```

- [ ] **Step 3: 扩展 `KnowledgeStore` 接口**

在 `src/store/knowledge-store.ts` 的 `historyCleanup` 之后、`close` 之前新增：

```typescript
  // ---- 会话状态 ----
  sessionStateGet(projectRoot: string): SessionState | undefined;
  sessionStateUpsert(state: SessionState): void;
  sessionStateRemove(projectRoot: string): void;
```

- [ ] **Step 4: 运行相关测试确认通过**

```bash
npm test -- src/store/migrations.test.ts
npm run typecheck
```

Expected: 现有迁移测试通过；类型检查无错误（此时 `SqliteKnowledgeStore` 尚未实现新方法，但因接口未在其他地方使用，类型检查可通过）。

- [ ] **Step 5: 提交**

```bash
git add src/store/migrations.ts src/common/types.ts src/store/knowledge-store.ts
git commit -m "feat(store): add session_state schema and interface"
```

---

## Task 2: `SqliteKnowledgeStore` 实现会话状态方法并测试

**Files:**
- Modify: `src/store/knowledge-store.impl.ts`
- Test: `src/store/knowledge-store.test.ts`

**Interfaces:**
- Consumes: `SessionState` 类型；`session_state` 表结构。
- Produces: `sessionStateGet/Upsert/Remove` 的完整实现。

- [ ] **Step 1: 写失败测试 `TC-UT-STORE-015`–`017`**

在 `src/store/knowledge-store.test.ts` 中新增 `describe('session state', () => { ... })`：

```typescript
  describe('session state', () => {
    // TC-UT-STORE-015: 应保存并读取会话状态
    it('TC-UT-STORE-015: should save and retrieve session state', () => {
      const state: SessionState = {
        project_root: '/tmp/project-a',
        active_file: 'src/main.ts',
        cursor_line: 42,
        cursor_col: 12,
        cursor_symbol: 'main',
      };
      store.sessionStateUpsert(state);
      const got = store.sessionStateGet('/tmp/project-a');
      expect(got).toBeDefined();
      expect(got!.active_file).toBe('src/main.ts');
      expect(got!.cursor_line).toBe(42);
      expect(got!.cursor_col).toBe(12);
      expect(got!.cursor_symbol).toBe('main');
    });

    // TC-UT-STORE-016: 应更新已有会话状态
    it('TC-UT-STORE-016: should update existing session state', () => {
      store.sessionStateUpsert({
        project_root: '/tmp/project-b',
        active_file: 'src/a.ts',
        cursor_line: 1,
        cursor_col: 1,
        cursor_symbol: null,
      });
      store.sessionStateUpsert({
        project_root: '/tmp/project-b',
        active_file: 'src/b.ts',
        cursor_line: 10,
        cursor_col: 5,
        cursor_symbol: 'foo',
      });
      const got = store.sessionStateGet('/tmp/project-b');
      expect(got!.active_file).toBe('src/b.ts');
      expect(got!.cursor_line).toBe(10);
    });

    // TC-UT-STORE-017: 应删除会话状态
    it('TC-UT-STORE-017: should remove session state', () => {
      store.sessionStateUpsert({
        project_root: '/tmp/project-c',
        active_file: 'src/c.ts',
        cursor_line: 3,
        cursor_col: 3,
        cursor_symbol: null,
      });
      store.sessionStateRemove('/tmp/project-c');
      expect(store.sessionStateGet('/tmp/project-c')).toBeUndefined();
    });
  });
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npm test -- src/store/knowledge-store.test.ts
```

Expected: 三个新测试失败，因为方法未实现。

- [ ] **Step 3: 实现 `sessionState*` 方法**

在 `src/store/knowledge-store.impl.ts` 的 `historyCleanup` 与 `close` 之间新增：

```typescript
  // ========== 会话状态 ==========

  sessionStateGet(projectRoot: string): SessionState | undefined {
    const row = this.db.prepare('SELECT * FROM session_state WHERE project_root = ?').get(projectRoot) as Record<string, unknown> | undefined;
    if (!row) return undefined;
    return {
      project_root: row.project_root as string,
      active_file: (row.active_file as string | null) ?? null,
      cursor_line: (row.cursor_line as number | null) ?? null,
      cursor_col: (row.cursor_col as number | null) ?? null,
      cursor_symbol: (row.cursor_symbol as string | null) ?? null,
      updated_at: (row.updated_at as string | undefined),
    };
  }

  sessionStateUpsert(state: SessionState): void {
    this.db.prepare(`
      INSERT OR REPLACE INTO session_state
        (project_root, active_file, cursor_line, cursor_col, cursor_symbol, updated_at)
      VALUES (?, ?, ?, ?, ?, datetime('now'))
    `).run(
      state.project_root,
      state.active_file ?? null,
      state.cursor_line ?? null,
      state.cursor_col ?? null,
      state.cursor_symbol ?? null,
    );
  }

  sessionStateRemove(projectRoot: string): void {
    this.db.prepare('DELETE FROM session_state WHERE project_root = ?').run(projectRoot);
  }
```

- [ ] **Step 4: 运行测试确认通过**

```bash
npm test -- src/store/knowledge-store.test.ts
npm run typecheck
```

Expected: 全部通过。

- [ ] **Step 5: 提交**

```bash
git add src/store/knowledge-store.impl.ts src/store/knowledge-store.test.ts src/common/types.ts
# 注意：若 Task 1 已提交 types.ts，这里只提交 impl 与 test 即可
git commit -m "feat(store): implement session state persistence"
```

---

## Task 3: `NodusShell` 关闭时保存会话状态

**Files:**
- Modify: `src/shell/nodus-shell.ts`

**Interfaces:**
- Consumes: `ContextManager.snapshot()`、`KnowledgeStore.sessionStateUpsert`。
- Produces: `shutdown()` 结束时把当前活动项目的会话状态写入数据库。

- [ ] **Step 1: 在 `shutdown()` 中保存会话状态**

在 `src/shell/nodus-shell.ts` 的 `shutdown()` 方法中，在 `this.store.close()` 之前加入：

```typescript
  async shutdown(): Promise<void> {
    console.log('[Nodus] Shutting down...');

    // 保存当前会话状态
    const ctx = this.contextMgr.snapshot();
    if (ctx.active_project_root) {
      this.store.sessionStateUpsert({
        project_root: ctx.active_project_root,
        active_file: ctx.active_file,
        cursor_line: ctx.cursor_line,
        cursor_col: ctx.cursor_col,
        cursor_symbol: ctx.cursor_symbol,
      });
    }

    this.fileWatcher.pause();
    await this.voicePipeline.stop();
    this.unsubscribeConfig?.();
    this.store.close();
    this.eventBus.clear();
    console.log('[Nodus] Goodbye.');
  }
```

- [ ] **Step 2: 运行测试确认无回归**

```bash
npm test -- src/shell/nodus-shell.test.ts
npm run typecheck
```

Expected: 现有测试通过（新增的 `sessionStateUpsert` 在 `SqliteKnowledgeStore` 中已实现）。

- [ ] **Step 3: 提交**

```bash
git add src/shell/nodus-shell.ts
git commit -m "feat(shell): save session state on shutdown"
```

---

## Task 4: `NodusShell` 打开项目时恢复会话状态

**Files:**
- Modify: `src/shell/nodus-shell.ts`

**Interfaces:**
- Consumes: `KnowledgeStore.sessionStateGet`、`
ContextManager.update`。
- Produces: `openProject()` 成功后会根据数据库中保存的 `session_state` 恢复文件与光标。

- [ ] **Step 1: 在 `openProject()` 成功后恢复会话状态**

在 `src/shell/nodus-shell.ts` 的 `openProject()` 方法中，在 `console.log('[Nodus] Project ready...')` 之前加入：

```typescript
      // 恢复该项目上次会话状态
      const session = this.store.sessionStateGet(projectPath);
      if (session?.active_file) {
        this.contextMgr.update({ kind: 'project_changed', root: projectPath });
        this.contextMgr.update({ kind: 'file_opened', path: session.active_file });
        if (session.cursor_line != null && session.cursor_col != null) {
          this.contextMgr.update({
            kind: 'cursor_moved',
            file: session.active_file,
            line: session.cursor_line,
            col: session.cursor_col,
            symbol: session.cursor_symbol,
          });
        }
      }
```

- [ ] **Step 2: 运行测试确认无回归**

```bash
npm test -- src/shell/nodus-shell.test.ts
npm run typecheck
```

Expected: 现有测试通过。

- [ ] **Step 3: 提交**

```bash
git add src/shell/nodus-shell.ts
git commit -m "feat(shell): restore session state when opening project"
```

---

## Task 5: 集成测试、README 更新与最终审查

**Files:**
- Modify: `src/shell/nodus-shell.test.ts`
- Modify: `readme.md`

- [ ] **Step 1: 新增会话恢复集成测试 `TC-UT-SH-008`**

在 `src/shell/nodus-shell.test.ts` 末尾新增：

```typescript
  // TC-UT-SH-008: 重启后应恢复项目、文件与光标位置
  it('TC-UT-SH-008: should restore session state after restart', async () => {
    const dbPath = join(tmpDir, 'session.db');
    const configPath = join(tmpDir, 'config.json');
    writeFileSync(configPath, JSON.stringify({
      projectPaths: [tinyProjectPath],
      dbPath,
      locale: 'zh-CN',
    }, null, 2));

    // 第一次启动：建立会话状态
    const configManager1 = new JsonConfigManager(configPath);
    const shell1 = new NodusShell(configManager1);
    await shell1.bootstrap();

    shell1.contextMgr.update({ kind: 'project_changed', root: tinyProjectPath });
    shell1.contextMgr.update({ kind: 'file_opened', path: join(tinyProjectPath, 'index.ts') });
    shell1.contextMgr.update({
      kind: 'cursor_moved',
      file: join(tinyProjectPath, 'index.ts'),
      line: 7,
      col: 3,
      symbol: 'greet',
    });

    await shell1.shutdown();

    // 第二次启动：验证恢复
    const configManager2 = new JsonConfigManager(configPath);
    const shell2 = new NodusShell(configManager2);
    await shell2.bootstrap();

    const ctx = shell2.contextMgr.snapshot();
    expect(ctx.active_project_root).toBe(tinyProjectPath);
    expect(ctx.active_file).toBe(join(tinyProjectPath, 'index.ts'));
    expect(ctx.cursor_line).toBe(7);
    expect(ctx.cursor_col).toBe(3);
    expect(ctx.cursor_symbol).toBe('greet');

    await shell2.shutdown();
  });
```

- [ ] **Step 2: 运行测试确认通过**

```bash
npm test -- src/shell/nodus-shell.test.ts
npm run typecheck
```

Expected: 全部通过。

- [ ] **Step 3: 运行全量测试**

```bash
npm test
```

Expected: 全部通过。

- [ ] **Step 4: 在 `readme.md` 中将 R1.7 标记为完成**

找到：

```markdown
- [ ] R1.7 会话恢复（重启后恢复项目、文件、光标位置）
```

改为：

```markdown
- [x] R1.7 会话恢复（重启后恢复项目、文件、光标位置）
```

- [ ] **Step 5: 提交**

```bash
git add src/shell/nodus-shell.test.ts readme.md
git commit -m "test(shell): add session restoration integration test and mark R1.7 complete"
```

- [ ] **Step 6: 最终审查**

确认：
1. `session_state` 表迁移正确注册为版本 2。
2. `SessionState` 类型与 `KnowledgeStore` 接口一致。
3. `SqliteKnowledgeStore` 实现 `sessionStateGet/Upsert/Remove` 并通过单元测试。
4. `NodusShell.shutdown()` 保存当前活动项目的会话状态。
5. `NodusShell.openProject()` 成功后恢复文件与光标。
6. `TC-UT-SH-008` 通过；全量测试通过。
7. `readme.md` R1.7 已打勾。

---

## Self-Review

1. **Spec coverage:** R1.7 需求是“重启后恢复项目、文件、光标位置”。任务覆盖：数据模型（Task 1）、持久化层（Task 2）、保存逻辑（Task 3）、恢复逻辑（Task 4）、集成测试与文档（Task 5）。无遗漏。
2. **Placeholder scan:** 无 TBD/TODO/"implement later"；所有代码与测试均完整给出。
3. **Type consistency:** `SessionState` 字段与 `Context` 对应字段一致；`sessionStateGet/Upsert/Remove` 签名在接口、实现、测试中一致。
