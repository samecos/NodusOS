# R1.8: 错误处理与降级卡片统一 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 统一所有模块错误（`NodusError` 子类）的终端呈现：当模块抛出错误时，`NodusShell` 将其转换为一张降级提示卡片，通过 `TerminalRenderer` 输出包含错误码、模块来源、友好说明与降级建议的结构化文本。

**Architecture:** 扩展 `UIRenderer` 卡片系统以支持 `error` 类型的卡片数据；新增 `DegradationCardBuilder` 工具函数把 `NodusError` 映射为卡片 payload；`NodusShell` 在事件总线 `error` 事件和查询/打开项目等边界处捕获异常，统一创建降级卡片并渲染。

**Tech Stack:** TypeScript, ESM, Vitest。

## Global Constraints

- 项目使用 ESM，`package.json` 中 `"type": "module"`。
- `tsconfig.json` 使用 `nodenext`、`strict: true`、`verbatimModuleSyntax: true`；类型导入必须写 `import type { ... }`。
- 测试使用 Vitest，`vitest.config.ts` 已配置 `globals: true`、`environment: 'node'`。
- 所有源码注释与文档主要使用中文；标识符保持英文。
- 测试用例沿用 `TC-UT-UI-NNN` / `TC-UT-SH-NNN` / `TC-UT-ERR-NNN` 编号风格。
- 每次任务结束需运行 `npm test` 与 `npm run typecheck` 并全部通过。
- 每个任务独立可提交，提交信息使用中文或英文 conventional commits。

---

## File Structure

- `src/ui/ui-renderer.ts` — 扩展 `CardKind` 与 `Card['data']` 联合类型以支持错误卡片。
- `src/ui/terminal-renderer.ts` — 实现 `renderNodusErrorCard` 私有方法并在 `renderCard` 中路由。
- `src/common/errors.ts` — 新增 `DegradationSuggestion` 映射，给常见错误码提供友好提示。
- `src/shell/nodus-shell.ts` — 在 `error` 事件路由与查询边界统一创建降级卡片。
- `src/ui/terminal-renderer.test.ts` — 新增错误卡片渲染测试。
- `src/shell/nodus-shell.test.ts` — 新增错误降级卡片集成测试。
- `readme.md` — R1.8 完成后在 TODO 列表打勾。

---

## Task 1: 扩展卡片系统支持 `error` 类型

**Files:**
- Modify: `src/ui/ui-renderer.ts`
- Modify: `src/ui/terminal-renderer.ts`
- Test: `src/ui/terminal-renderer.test.ts`

**Interfaces:**
- Consumes: `NodusError` from `src/common/errors.js`。
- Produces: `CardKind` 新增 `'error'`；`Card['data']` 新增 `{ kind: 'error'; error: NodusError; module: string }`；`TerminalRenderer.renderCard` 能渲染该类型。

- [ ] **Step 1: 写失败测试 `TC-UT-UI-022`——应渲染错误卡片**

在 `src/ui/terminal-renderer.test.ts` 末尾新增：

```typescript
  // TC-UT-UI-022: 应渲染 NodusError 降级卡片
  it('TC-UT-UI-022: should render NodusError degradation card', () => {
    const error = new CodeIntelError(CodeIntelError.NO_PARSER, 'No parser available');
    const card = renderer.createCard('err-1', '出错了', {
      kind: 'error',
      module: 'code_intel',
      error,
    });
    const out = renderer.renderCard(card);
    expect(out).toContain('No parser available');
    expect(out).toContain(CodeIntelError.NO_PARSER);
    expect(out).toContain('code_intel');
  });
```

需要导入 `CodeIntelError`：

```typescript
import { CodeIntelError } from '../common/errors.js';
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npm test -- src/ui/terminal-renderer.test.ts
```

Expected: `TC-UT-UI-022` 失败，因为 `Card['data']` 联合类型不包含错误 payload，且 `renderCard` 没有对应分支。

- [ ] **Step 3: 扩展类型并实现渲染**

在 `src/ui/ui-renderer.ts` 中：

1. `CardKind` 追加 `'error'`。
2. `Card['data']` 联合类型追加 `{ kind: 'error'; error: NodusError; module: string }`。

```typescript
import type { NodusError } from '../common/errors.js';

export type CardKind =
  | 'symbol_list'
  // ... 现有种类 ...
  | 'error';

export interface Card {
  id: string;
  kind: CardKind;
  title: string;
  data:
    | QueryResult
    | IntentError
    | ProjectMeta
    | QueryIntent[]
    | { title: string; body: string }
    | { kind: 'error'; error: NodusError; module: string };
  // ...
}
```

在 `src/ui/terminal-renderer.ts` 中：

1. 在 `renderCard` 的 `if ('kind' in card.data ...)` 分支之后，新增错误卡片分支：

```typescript
    if ('kind' in card.data && card.data.kind === 'error') {
      return this.renderNodusErrorCard(card.data.error, card.data.module);
    }
```

2. 新增私有方法：

```typescript
  private renderNodusErrorCard(error: NodusError, module: string): string {
    const suggestion = DEGRADATION_SUGGESTIONS[error.code] ?? '系统遇到意外问题，请稍后重试或查看日志。';
    let out = c('\n⚠ 运行降级提示', YELLOW) + '\n';
    out += `  来源模块: ${c(module, DIM)}\n`;
    out += `  错误码:   ${c(error.code, RED)}\n`;
    out += `  说明:     ${error.message}\n`;
    out += `  建议:     ${c(suggestion, GREEN)}\n`;
    return out;
  }
```

3. 在文件顶部定义 `DEGRADATION_SUGGESTIONS`：

```typescript
const DEGRADATION_SUGGESTIONS: Record<string, string> = {
  [CodeIntelError.UNSUPPORTED_FILE]: '该文件类型暂不支持解析，可尝试用文本查询其他文件。',
  [CodeIntelError.NO_PARSER]: '解析器未就绪，请确认 native 依赖已正确构建。',
  [CodeIntelError.PARSE_FAILED]: '文件解析失败，请检查语法或稍后重试。',
  [CodeIntelError.NOT_INDEXED]: '文件尚未索引，可等待索引完成后再查询。',
  [EnvError.UNKNOWN_PROJECT_TYPE]: '无法识别项目类型，请检查项目结构或手动指定运行时。',
  [EnvError.RUNTIME_INSTALL_FAILED]: '运行时安装失败，可尝试手动安装后重启。',
  [EnvError.DEP_INSTALL_FAILED]: '依赖安装失败，建议检查网络或包管理器日志。',
  [EnvError.COMMAND_FAILED]: '外部命令执行失败，请确认命令可用。',
  [EnvError.RUNTIME_NOT_FOUND]: '未找到指定运行时，请先安装对应版本。',
  [GitError.NOT_A_REPO]: '当前目录不是 git 仓库，无法提供变更历史。',
  [GitError.COMMAND_FAILED]: 'git 命令执行失败，请确认 git 已安装且仓库状态正常。',
  [VoiceError.MICROPHONE_NOT_AVAILABLE]: '麦克风不可用，已切换为文本输入模式。',
  [VoiceError.AUDIO_FILE_NOT_FOUND]: '音频文件不存在，请确认录音已完成。',
  [VoiceError.TRANSCRIPTION_FAILED]: '语音识别失败，请重试或切换为键盘输入。',
};
```

并导入 `CodeIntelError`, `EnvError`, `GitError`, `VoiceError`：

```typescript
import { CodeIntelError, EnvError, GitError, VoiceError } from '../common/errors.js';
```

- [ ] **Step 4: 运行测试确认通过**

```bash
npm test -- src/ui/terminal-renderer.test.ts
npm run typecheck
```

Expected: 全部通过。

- [ ] **Step 5: 提交**

```bash
git add src/ui/ui-renderer.ts src/ui/terminal-renderer.ts src/ui/terminal-renderer.test.ts
git commit -m "feat(ui): support error degradation cards in terminal renderer"
```

---

## Task 2: 统一 `NodusShell` 错误事件为降级卡片

**Files:**
- Modify: `src/shell/nodus-shell.ts`

**Interfaces:**
- Consumes: `ErrorEvent` from事件总线；`UIRenderer.createCard` / `renderCard`。
- Produces: `NodusShell` 在收到 `error` 事件时创建并输出一张降级卡片。

- [ ] **Step 1: 修改 `registerEventRoutes` 中的 `error` 处理**

将 `src/shell/nodus-shell.ts` 中：

```typescript
    // 错误事件 → 统一日志与降级提示
    this.eventBus.on('error', (event) => {
      console.error(`[Nodus] Error from ${event.module}: ${event.error.message} (${event.error.code})`);
    });
```

替换为：

```typescript
    // 错误事件 → 统一降级卡片
    this.eventBus.on('error', (event) => {
      const card = this.uiRenderer.createCard(
        `error-${event.error.code}`,
        '运行降级提示',
        { kind: 'error', error: event.error, module: event.module },
      );
      console.error(this.uiRenderer.renderCard(card));
    });
```

- [ ] **Step 2: 运行测试确认无回归**

```bash
npm test -- src/shell/nodus-shell.test.ts
npm run typecheck
```

Expected: 现有测试通过（事件总线错误事件目前只在测试外使用）。

- [ ] **Step 3: 提交**

```bash
git add src/shell/nodus-shell.ts
git commit -m "feat(shell): render degradation card on error events"
```

---

## Task 3: 在查询与项目打开边界捕获未处理异常

**Files:**
- Modify: `src/shell/nodus-shell.ts`

**Interfaces:**
- Consumes: `NodusError` 子类；`handleQueryFormatted`、`openProject` 内部可能抛出的异常。
- Produces: 任何未捕获的 `NodusError` 都会通过事件总线发出并降级为卡片；非 `NodusError` 也被包装后降级。

- [ ] **Step 1: 添加 `handleQueryFormatted` 异常捕获**

在 `src/shell/nodus-shell.ts` 的 `handleQueryFormatted` 方法中，将整个方法体（除了初始事件发射）包裹在 `try/catch` 中：

```typescript
  async handleQueryFormatted(text: string): Promise<string> {
    this.eventBus.emit({ kind: 'query:received', text });

    try {
      const context = this.contextMgr.snapshot();
      const result = this.intentEngine.parse(
        { source: 'text', text, locale: this.config.locale ?? 'zh-CN' },
        context,
      );

      if ('kind' in result && !('intentType' in result)) {
        return this.uiRenderer.renderError(result as unknown as import('../intent/intent-engine.js').IntentError);
      }

      const intent = result as QueryIntent;
      this.contextMgr.recordQuery(text, intent.intentType);
      const queryResult = await this.codeIntel.query(intent);
      this.eventBus.emit({ kind: 'query:result', result: queryResult });

      this.store.historyRecord({
        raw_text: text,
        intent_type: intent.intentType,
        confidence: intent.confidence,
        latency_ms: 0,
        result_count: this.countResults(queryResult),
        timestamp: new Date().toISOString(),
      });

      return this.uiRenderer.render(queryResult);
    } catch (err) {
      const nodusErr = err instanceof NodusError
        ? err
        : new NodusError('SHELL_QUERY_FAILED', err instanceof Error ? err.message : String(err), { cause: err });
      this.eventBus.emit({ kind: 'error', module: 'shell', error: nodusErr });
      return this.uiRenderer.renderCard(this.uiRenderer.createCard(
        `error-${nodusErr.code}`,
        '查询失败',
        { kind: 'error', error: nodusErr, module: 'shell' },
      ));
    }
  }
```

- [ ] **Step 2: 添加 `openProject` 异常捕获**

`openProject` 已有 `try/catch`，将 `console.error` 替换为事件发射 + 卡片渲染：

```typescript
    } catch (err) {
      const nodusErr = err instanceof NodusError
        ? err
        : new EnvError(EnvError.COMMAND_FAILED, `Failed to open project ${projectPath}: ${err instanceof Error ? err.message : String(err)}`, { cause: err });
      this.eventBus.emit({ kind: 'error', module: 'environment', error: nodusErr });
      console.error(this.uiRenderer.renderCard(this.uiRenderer.createCard(
        `error-${nodusErr.code}`,
        '打开项目失败',
        { kind: 'error', error: nodusErr, module: 'environment' },
      )));
    }
```

- [ ] **Step 3: 导入 `NodusError`**

在 `src/shell/nodus-shell.ts` 顶部新增：

```typescript
import { NodusError, EnvError } from '../common/errors.js';
```

- [ ] **Step 4: 运行测试确认通过**

```bash
npm test -- src/shell/nodus-shell.test.ts
npm run typecheck
```

Expected: 全部通过。

- [ ] **Step 5: 提交**

```bash
git add src/shell/nodus-shell.ts
git commit -m "feat(shell): catch query and openProject errors and render degradation cards"
```

---

## Task 4: 错误降级卡片集成测试

**Files:**
- Modify: `src/shell/nodus-shell.test.ts`

**Interfaces:**
- Consumes: `NodusShell.handleQueryFormatted`、`CodeIntelError`、`EventBus`。
- Produces: 新增 `TC-UT-SH-009` 验证查询抛出错误时返回包含错误码的降级卡片文本。

- [ ] **Step 1: 写失败测试 `TC-UT-SH-009`**

在 `src/shell/nodus-shell.test.ts` 末尾新增：

```typescript
  // TC-UT-SH-009: 查询异常时应返回降级卡片
  it('TC-UT-SH-009: should return degradation card when query fails', async () => {
    const configPath = join(tmpDir, 'config.json');
    writeFileSync(configPath, JSON.stringify({
      projectPaths: [tinyProjectPath],
      dbPath: ':memory:',
      locale: 'zh-CN',
    }, null, 2));

    currentConfigManager = new JsonConfigManager(configPath);
    shell = new NodusShell(currentConfigManager);
    await shell.bootstrap();

    // 用一个会导致解析失败的文件路径触发 CodeIntelError
    const result = await shell.handleQueryFormatted('refundOrder在哪里定义的');
    expect(result).toContain('查询');
    expect(result).toContain('SHELL_QUERY_FAILED');

    await shell.shutdown();
  });
```

- [ ] **Step 2: 运行测试确认失败**

```bash
npm test -- src/shell/nodus-shell.test.ts
```

Expected: `TC-UT-SH-009` 可能通过也可能失败，取决于查询是否稳定触发错误。如果失败，说明需要更稳定的错误触发方式；请调整测试以直接替换 `codeIntel.query` 为抛出 `CodeIntelError` 的 mock：

```typescript
    const originalQuery = shell.codeIntel.query.bind(shell.codeIntel);
    shell.codeIntel.query = async () => {
      throw new CodeIntelError(CodeIntelError.NOT_INDEXED, 'project not indexed');
    };

    const result = await shell.handleQueryFormatted('refundOrder在哪里定义的');
    expect(result).toContain(CodeIntelError.NOT_INDEXED);
    expect(result).toContain('code_intel');

    shell.codeIntel.query = originalQuery;
```

- [ ] **Step 3: 运行测试确认通过**

```bash
npm test -- src/shell/nodus-shell.test.ts
npm run typecheck
```

Expected: 全部通过。

- [ ] **Step 4: 提交**

```bash
git add src/shell/nodus-shell.test.ts
git commit -m "test(shell): add degradation card integration test"
```

---

## Task 5: 全量测试、README 更新与最终审查

**Files:**
- Modify: `readme.md`

- [ ] **Step 1: 运行全量测试与类型检查**

```bash
npm test
npm run typecheck
```

Expected: 全部通过。

- [ ] **Step 2: 在 `readme.md` 中将 R1.8 标记为完成**

找到：

```markdown
- [ ] R1.8 错误处理与降级卡片统一
```

改为：

```markdown
- [x] R1.8 错误处理与降级卡片统一
```

- [ ] **Step 3: 提交**

```bash
git add readme.md
git commit -m "docs: mark R1.8 error handling and degradation cards complete"
```

- [ ] **Step 4: 最终审查**

确认：
1. `CardKind` 与 `Card['data']` 支持 `error` 类型。
2. `TerminalRenderer` 能渲染包含错误码、模块、消息、建议的降级卡片。
3. `NodusShell` 在事件总线 `error` 事件、查询异常、项目打开异常时均输出降级卡片。
4. 新增 `TC-UT-UI-022` 与 `TC-UT-SH-009` 通过。
5. 全量测试通过。
6. `readme.md` R1.8 已打勾。

---

## Self-Review

1. **Spec coverage:** R1.8 需求是“错误处理与降级卡片统一”。任务覆盖：卡片类型扩展（Task 1）、错误事件卡片化（Task 2）、边界异常捕获（Task 3）、集成测试（Task 4）、文档收尾（Task 5）。无遗漏。
2. **Placeholder scan:** 无 TBD/TODO/"implement later"；所有代码与测试均完整给出。
3. **Type consistency:** `error` 卡片 payload 使用 `{ kind: 'error'; error: NodusError; module: string }`，在 `ui-renderer.ts`、`terminal-renderer.ts`、`nodus-shell.ts` 中一致。
